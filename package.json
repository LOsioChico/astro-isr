{
  "name": "astro-isr",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "pages:dev": "astro build && wrangler pages dev ./dist --port 4321",
    "pages:deploy": "astro build && wrangler pages deploy ./dist",
    "worker:dev": "astro build && wrangler dev --port 4321",
    "worker:deploy": "astro build && wrangler deploy",
    "build": "astro build",
    "preview": "astro preview"
  },
  "dependencies": {
    "astro": "^5.8.1",
    "@astrojs/cloudflare": "^12.5.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0"
  }
}
